---
export const prerender = false;

/**
 * HIDDEN, PENDING A DECISION ABOUT WHO IT IS FOR.
 *
 * The pitch to somebody running a club, a research class or a fair season:
 * what the platform looks like from the side of the adult who would adopt it
 * rather than the student who would use it.
 *
 * It is hidden rather than removed. The argument for it stands — two readers
 * arrive at the home page and only one of them is addressed — but the page
 * was written before the product had been shown to a single teacher, and a
 * pitch that has never met its audience is a guess. It should come back when
 * there is something to say that a teacher has already agreed with.
 *
 * Recorded in the brief. The prose is intact in the history.
 */
return Astro.redirect('/', 301);
---
