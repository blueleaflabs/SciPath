{
  "name": "scipath",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:public-only": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:contrast": "node tests/contrast.mjs",
    "test:static": "node tests/no-db-in-static.mjs",
    "test": "npm run test:dates && npm run test:drift && npm run test:contrast && npm run test:static && npm run test:tokens && npm run test:config && npm run test:tenant && npm run test:routes",
    "test:tokens": "node tests/tokens-only.mjs",
    "test:config": "node tests/config-secrets.mjs",
    "seed:demo": "node scripts/seed-demo.mjs",
    "db:reset": "supabase db reset",
    "test:tenant": "node tests/tenant-resolution.mjs",
    "test:routes": "node tests/tenant-routes.mjs",
    "test:output": "node tests/tenant-output.mjs",
    "verify": "npm run build && npm test && npm run test:output",
    "test:dates": "node --experimental-strip-types tests/date-order.mjs",
    "test:drift": "node tests/schema-drift.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.0",
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.4.0",
    "@supabase/ssr": "^0.7.0",
    "@supabase/supabase-js": "^2.58.0",
    "astro": "^5.12.0",
    "js-yaml": "^4.1.0",
    "marked": "^18.0.7"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "supabase": "^2.111.0"
  }
}
