{
  "name": "scipath",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:public-only": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:contrast": "node tests/contrast.mjs",
    "test:static": "node tests/no-db-in-static.mjs",
    "test": "npm run test:contrast && npm run test:static && npm run test:tokens",
    "test:tokens": "node tests/tokens-only.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.11",
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.12.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "supabase": "^2.111.0"
  }
}
